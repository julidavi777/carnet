import { ContactTitlePipe } from './contact-title.pipe';

describe('ContactTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
